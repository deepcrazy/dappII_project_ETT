const EventTicketingToken = artifacts.require("EventTicketingToken.sol")

contract("EventTicketingToken", accounts => {

    it("should return the owner of tokenId", async function() {
        console.log("Test cases has to done in near future.!");
    })
})
